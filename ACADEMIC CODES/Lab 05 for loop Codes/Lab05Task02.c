#include <stdio.h>

int main()
{

    int n=10;

    //printf("Enter the value of n: ");

    for (; n > 0;)
        scanf("%d", &n);

    printf("\nValue of i after end of loop: %d\n",n);

    return 0;
}

