    // int sum = 0;
    // int mul = 1;

    // for (i = 0; i < n; i++)
    // {
    //     sum = sum + a[i];
    //     mul = mul * a[i];
    // }
    // printf("\n\tsum: %d\n\tMul: %d\n\n",sum, mul);